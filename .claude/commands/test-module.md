---
description: Run tests for a specific module (e.g. sync, config, github, error)
argument-hint: [module]
---

Run tests for the specified module:

```bash
cargo test $ARGUMENTS::tests --verbose
```

Show the test output and summarize pass/fail results.
